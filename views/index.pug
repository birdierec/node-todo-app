doctype html
html(lang="en")
  head
    title= 'BOOOM'
    script(type='text/javascript').
      if (foo) bar(1 + 5)
  body
    h1 testing bruh
    #container.col
      if greet
        p HELLO #{person}
      else
        p Get on it!
      p.
        Pug is a terse and simple templating language with a
        strong focus on performance and powerful features.
      hey #{name} fdsafdsafds something new

    each item in testlist
        li= item.name

    div wewewew

    form(method='POST', action='/api/greeting')
        input(name='test')
        button Submit
